{
  "name": "lego-ugv",
  "version": "0.0.1",
  "description": "A Unmanned Ground Vehicule in Lego Mindstorm and BrickPI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd webapp && npm start",
    "ssh": "ssh robot@ev3dev.local",
    "sync:build": "cd webapp && npm run build && cd .. && npm run sync",
    "sync": "./bin/sync.sh once ./server robot@ev3dev.local server",
    "watch": "./bin/sync.sh watch ./server robot@ev3dev.local server",
    "ssh-close": "./bin/sync.sh close robot@ev3dev.local"
  },
  "keywords": [
    "lego",
    "brickpi",
    "nodejs",
    "ev3dev",
    "cyclejs"
  ],
  "devDependencies": {
    "stylelint-config-standard": "^16.0.0"
  },
  "author": "Pierre Chabiland",
  "license": "Beerware",
  "dependencies": {}
}
