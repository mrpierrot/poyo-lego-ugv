{
  "name": "lego-ugv",
  "version": "0.0.1",
  "description": "A Unmanned Ground Vehicule in Lego Mindstorm and BrickPI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd webapp && npm start",
    "ssh": "ssh robot@ev3dev.local",
    "sync": "rsync -av --partial --progress --rsh=ssh server robot@ev3dev.local:~/",
    "sync:build": "cd webapp && npm run build && cd .. && npm run sync"
  },
  "keywords": [
    "lego",
    "brickpi",
    "nodejs",
    "ev3dev",
    "cyclejs"
  ],
  "devDependencies": {
    "stylelint-config-standard": "^16.0.0"
  },
  "author": "Pierre Chabiland",
  "license": "Beerware",
  "dependencies": {}
}
